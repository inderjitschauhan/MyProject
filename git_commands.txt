cd "C:\Users\inder\Documents\Final Project\New_Code"
git clone https://github.com/inderjitschauhan/MyProject.git
cd MyProject
git remote -v
git config --global user.name "inderjitschauhan"
git config --global user.email "inderjits@iisc.ac.in"
git config --global user.name
git config --global user.email
git status
echo "# MyProject" > README.md
git add .
git commit -m "Added README file"
git branch
git push origin main
git log --oneline
git pull origin main

cd "C:\Users\inder\Documents\Final Project\New_Code\MyProject"

# 1️⃣ Get the latest version from GitHub
git pull origin main

# 2️⃣ Make your changes (edit, add, or delete files)
# e.g. open files in VS Code or any editor

# 3️⃣ Check what changed
git status

# 4️⃣ Stage your changes
git add .

# 5️⃣ Commit them with a message
git commit -m "Your description of the change"

# 6️⃣ Push back to GitHub
git push origin main

